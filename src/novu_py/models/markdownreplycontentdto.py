"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .filerefdto import FileRefDto, FileRefDtoTypedDict
from novu_py.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class MarkdownReplyContentDtoTypedDict(TypedDict):
    markdown: str
    r"""Plain text or markdown body. Converted to the platform-native format by Novu."""
    files: NotRequired[List[FileRefDtoTypedDict]]
    r"""Optional attachments (max 15). Each file needs exactly one of data or url."""


class MarkdownReplyContentDto(BaseModel):
    markdown: str
    r"""Plain text or markdown body. Converted to the platform-native format by Novu."""

    files: Optional[List[FileRefDto]] = None
    r"""Optional attachments (max 15). Each file needs exactly one of data or url."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["files"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
