"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from novu_py.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class HumanSignalDtoType(str, Enum):
    HUMAN = "human"


class Kind(str, Enum):
    r"""Interaction verb queued by `ctx.ask` / `ctx.approve` / `ctx.choose` / `ctx.tell`."""

    ASK = "ask"
    APPROVE = "approve"
    CHOOSE = "choose"
    TELL = "tell"


HumanSignalDtoToTypedDict = TypeAliasType(
    "HumanSignalDtoToTypedDict", Union[str, List[str]]
)
r"""Novu subscriberId(s) allowed to settle this interaction (max 50). First valid answer wins. When omitted, the conversation subscriber is used. Subscriber ids only — not workflow topic recipients."""


HumanSignalDtoTo = TypeAliasType("HumanSignalDtoTo", Union[str, List[str]])
r"""Novu subscriberId(s) allowed to settle this interaction (max 50). First valid answer wins. When omitted, the conversation subscriber is used. Subscriber ids only — not workflow topic recipients."""


class HumanSignalDtoTypedDict(TypedDict):
    type: HumanSignalDtoType
    kind: Kind
    r"""Interaction verb queued by `ctx.ask` / `ctx.approve` / `ctx.choose` / `ctx.tell`."""
    prompt: str
    r"""Question, action description, or one-way message shown to the human."""
    request_id: str
    r"""Client-minted id returned by the framework helper; echoed on `ctx.humanResponse.requestId`."""
    options: NotRequired[List[str]]
    r"""Choice labels — required for `choose`, ignored otherwise."""
    from_: NotRequired[str]
    r"""Attribution label rendered in the card."""
    ttl_seconds: NotRequired[float]
    r"""Seconds until the interaction expires. Default 86400 (24h), max 259200."""
    to: NotRequired[HumanSignalDtoToTypedDict]
    r"""Novu subscriberId(s) allowed to settle this interaction (max 50). First valid answer wins. When omitted, the conversation subscriber is used. Subscriber ids only — not workflow topic recipients."""


class HumanSignalDto(BaseModel):
    type: HumanSignalDtoType

    kind: Kind
    r"""Interaction verb queued by `ctx.ask` / `ctx.approve` / `ctx.choose` / `ctx.tell`."""

    prompt: str
    r"""Question, action description, or one-way message shown to the human."""

    request_id: Annotated[str, pydantic.Field(alias="requestId")]
    r"""Client-minted id returned by the framework helper; echoed on `ctx.humanResponse.requestId`."""

    options: Optional[List[str]] = None
    r"""Choice labels — required for `choose`, ignored otherwise."""

    from_: Annotated[Optional[str], pydantic.Field(alias="from")] = None
    r"""Attribution label rendered in the card."""

    ttl_seconds: Annotated[Optional[float], pydantic.Field(alias="ttlSeconds")] = None
    r"""Seconds until the interaction expires. Default 86400 (24h), max 259200."""

    to: Optional[HumanSignalDtoTo] = None
    r"""Novu subscriberId(s) allowed to settle this interaction (max 50). First valid answer wins. When omitted, the conversation subscriber is used. Subscriber ids only — not workflow topic recipients."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["options", "from", "ttlSeconds", "to"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    HumanSignalDto.model_rebuild()
except NameError:
    pass
