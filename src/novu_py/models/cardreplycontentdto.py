"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .filerefdto import FileRefDto, FileRefDtoTypedDict
from novu_py.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Any, Dict, List, Optional
from typing_extensions import NotRequired, TypedDict


class CardReplyContentDtoTypedDict(TypedDict):
    card: Dict[str, Any]
    r"""Interactive card as a Chat SDK element tree (`type: \"card\"`). Prefer building with `@novu/framework` Card helpers when using TypeScript."""
    files: NotRequired[List[FileRefDtoTypedDict]]
    r"""Optional attachments (max 15). Supported alongside cards on platforms that allow it (e.g. WhatsApp)."""


class CardReplyContentDto(BaseModel):
    card: Dict[str, Any]
    r"""Interactive card as a Chat SDK element tree (`type: \"card\"`). Prefer building with `@novu/framework` Card helpers when using TypeScript."""

    files: Optional[List[FileRefDto]] = None
    r"""Optional attachments (max 15). Supported alongside cards on platforms that allow it (e.g. WhatsApp)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["files"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
