"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .agentmcpserverdto import AgentMcpServerDto, AgentMcpServerDtoTypedDict
from .agenttooldto import AgentToolDto, AgentToolDtoTypedDict
from novu_py.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ManagedRuntimeResponseDtoTypedDict(TypedDict):
    provider_id: str
    integration_id: str
    external_agent_id: str
    external_environment_id: NotRequired[str]
    r"""The provider-side environment that hosts this agent. Hydrated from the linked integration credentials. Absent when the integration has not been provisioned."""
    external_workspace_id: NotRequired[str]
    r"""The provider-side workspace id used in console deep links. Defaults to `'default'` (the auto-created Default Workspace). Hydrated from the linked integration credentials."""
    console_url: NotRequired[str]
    r"""Deep link to the agent in the provider console (e.g. platform.claude.com)."""
    tools: NotRequired[List[AgentToolDtoTypedDict]]
    r"""Live tools enabled on the managed-runtime provider for this agent. Sourced from the provider on read; omitted when the provider is temporarily unreachable."""
    mcp_servers: NotRequired[List[AgentMcpServerDtoTypedDict]]
    r"""MCP servers enabled on this agent, projected onto the runtime catalog `{ externalId, name, url }` shape. Mongo is the source of truth; the projection matches what the provider sees. Omitted when the provider read fails after a managed-agent write."""
    system_prompt: NotRequired[str]
    r"""System prompt used when invoking the agent."""


class ManagedRuntimeResponseDto(BaseModel):
    provider_id: Annotated[str, pydantic.Field(alias="providerId")]

    integration_id: Annotated[str, pydantic.Field(alias="integrationId")]

    external_agent_id: Annotated[str, pydantic.Field(alias="externalAgentId")]

    external_environment_id: Annotated[
        Optional[str], pydantic.Field(alias="externalEnvironmentId")
    ] = None
    r"""The provider-side environment that hosts this agent. Hydrated from the linked integration credentials. Absent when the integration has not been provisioned."""

    external_workspace_id: Annotated[
        Optional[str], pydantic.Field(alias="externalWorkspaceId")
    ] = None
    r"""The provider-side workspace id used in console deep links. Defaults to `'default'` (the auto-created Default Workspace). Hydrated from the linked integration credentials."""

    console_url: Annotated[Optional[str], pydantic.Field(alias="consoleUrl")] = None
    r"""Deep link to the agent in the provider console (e.g. platform.claude.com)."""

    tools: Optional[List[AgentToolDto]] = None
    r"""Live tools enabled on the managed-runtime provider for this agent. Sourced from the provider on read; omitted when the provider is temporarily unreachable."""

    mcp_servers: Annotated[
        Optional[List[AgentMcpServerDto]], pydantic.Field(alias="mcpServers")
    ] = None
    r"""MCP servers enabled on this agent, projected onto the runtime catalog `{ externalId, name, url }` shape. Mongo is the source of truth; the projection matches what the provider sees. Omitted when the provider read fails after a managed-agent write."""

    system_prompt: Annotated[Optional[str], pydantic.Field(alias="systemPrompt")] = None
    r"""System prompt used when invoking the agent."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "externalEnvironmentId",
                "externalWorkspaceId",
                "consoleUrl",
                "tools",
                "mcpServers",
                "systemPrompt",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    ManagedRuntimeResponseDto.model_rebuild()
except NameError:
    pass
