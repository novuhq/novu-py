"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from novu_py.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class AddAgentIntegrationRequestDtoTypedDict(TypedDict):
    integration_identifier: NotRequired[str]
    r"""The integration identifier (same as in the integration store), not the internal document _id."""
    provider_id: NotRequired[str]
    r"""Provider ID to auto-create a dedicated integration (e.g. novu-agent-email). When set, the server creates the integration if one does not already exist for this agent."""


class AddAgentIntegrationRequestDto(BaseModel):
    integration_identifier: Annotated[
        Optional[str], pydantic.Field(alias="integrationIdentifier")
    ] = None
    r"""The integration identifier (same as in the integration store), not the internal document _id."""

    provider_id: Annotated[Optional[str], pydantic.Field(alias="providerId")] = None
    r"""Provider ID to auto-create a dedicated integration (e.g. novu-agent-email). When set, the server creates the integration if one does not already exist for this agent."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["integrationIdentifier", "providerId"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    AddAgentIntegrationRequestDto.model_rebuild()
except NameError:
    pass
