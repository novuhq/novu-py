"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .channeltypeenum import ChannelTypeEnum
from novu_py.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class AgentIntegrationResponseIntegrationDtoTypedDict(TypedDict):
    id: str
    r"""Integration document _id."""
    identifier: str
    r"""The integration identifier (matches the integration store), not the internal MongoDB _id."""
    name: str
    provider_id: str
    active: bool
    channel: NotRequired[ChannelTypeEnum]
    r"""Channel type through which the message is sent"""
    shared_inbound_address: NotRequired[str]
    r"""The Novu shared inbox address for this agent. Set whenever the cloud shared-inbox feature is enabled. The dashboard uses this as the headline inbound address and to render the shared inbox row in the inbox list."""
    default_sender_name: NotRequired[str]
    r"""Default email From display name for this agent (NovuAgent integrations only). Falls back to the agent name when not explicitly stored on the integration credentials."""
    shared_inbox_disabled: NotRequired[bool]
    r"""When true, the worker drops inbound mail addressed to this agent on the shared `agentconnect.sh` domain. Custom-domain routes still deliver. Meaningful on cloud-enabled NovuAgent integrations; on self-hosted it is set defensively at provisioning time and is effectively redundant."""


class AgentIntegrationResponseIntegrationDto(BaseModel):
    id: Annotated[str, pydantic.Field(alias="_id")]
    r"""Integration document _id."""

    identifier: str
    r"""The integration identifier (matches the integration store), not the internal MongoDB _id."""

    name: str

    provider_id: Annotated[str, pydantic.Field(alias="providerId")]

    active: bool

    channel: Optional[ChannelTypeEnum] = None
    r"""Channel type through which the message is sent"""

    shared_inbound_address: Annotated[
        Optional[str], pydantic.Field(alias="sharedInboundAddress")
    ] = None
    r"""The Novu shared inbox address for this agent. Set whenever the cloud shared-inbox feature is enabled. The dashboard uses this as the headline inbound address and to render the shared inbox row in the inbox list."""

    default_sender_name: Annotated[
        Optional[str], pydantic.Field(alias="defaultSenderName")
    ] = None
    r"""Default email From display name for this agent (NovuAgent integrations only). Falls back to the agent name when not explicitly stored on the integration credentials."""

    shared_inbox_disabled: Annotated[
        Optional[bool], pydantic.Field(alias="sharedInboxDisabled")
    ] = None
    r"""When true, the worker drops inbound mail addressed to this agent on the shared `agentconnect.sh` domain. Custom-domain routes still deliver. Meaningful on cloud-enabled NovuAgent integrations; on self-hosted it is set defensively at provisioning time and is effectively redundant."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "channel",
                "sharedInboundAddress",
                "defaultSenderName",
                "sharedInboxDisabled",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    AgentIntegrationResponseIntegrationDto.model_rebuild()
except NameError:
    pass
