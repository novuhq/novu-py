"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .managedruntimedto import ManagedRuntimeDto, ManagedRuntimeDtoTypedDict
from enum import Enum
from novu_py.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Runtime(str, Enum):
    SELF_HOSTED = "self-hosted"
    MANAGED = "managed"


class CreateAgentRequestDtoTypedDict(TypedDict):
    name: str
    r"""Required when not adopting an existing managed agent (i.e. when managedRuntime.externalAgentId is absent). Optional in adopt mode where the name is resolved from the provider."""
    identifier: str
    r"""Required when not adopting an existing managed agent. Auto-generated from the provider agent name when omitted in adopt mode."""
    description: NotRequired[str]
    active: NotRequired[bool]
    runtime: NotRequired[Runtime]
    managed_runtime: NotRequired[ManagedRuntimeDtoTypedDict]


class CreateAgentRequestDto(BaseModel):
    name: str
    r"""Required when not adopting an existing managed agent (i.e. when managedRuntime.externalAgentId is absent). Optional in adopt mode where the name is resolved from the provider."""

    identifier: str
    r"""Required when not adopting an existing managed agent. Auto-generated from the provider agent name when omitted in adopt mode."""

    description: Optional[str] = None

    active: Optional[bool] = True

    runtime: Optional[Runtime] = None

    managed_runtime: Annotated[
        Optional[ManagedRuntimeDto], pydantic.Field(alias="managedRuntime")
    ] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["description", "active", "runtime", "managedRuntime"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    CreateAgentRequestDto.model_rebuild()
except NameError:
    pass
