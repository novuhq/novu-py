"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from novu_py.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class GrafanaOnCallIntegrationEndpointDtoTypedDict(TypedDict):
    url: str
    r"""Grafana IRM/OnCall incoming-webhook (Formatted Webhook) integration URL. The routing secret is embedded in the URL path. Encrypted at rest on the channel endpoint (`endpoint` field)."""
    auth_token: NotRequired[str]
    r"""Optional Grafana service account bearer token, required when the integration enforces authenticated ingestion. Encrypted at rest on the channel endpoint (`endpoint` field)."""


class GrafanaOnCallIntegrationEndpointDto(BaseModel):
    url: str
    r"""Grafana IRM/OnCall incoming-webhook (Formatted Webhook) integration URL. The routing secret is embedded in the URL path. Encrypted at rest on the channel endpoint (`endpoint` field)."""

    auth_token: Annotated[Optional[str], pydantic.Field(alias="authToken")] = None
    r"""Optional Grafana service account bearer token, required when the integration enforces authenticated ingestion. Encrypted at rest on the channel endpoint (`endpoint` field)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["authToken"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    GrafanaOnCallIntegrationEndpointDto.model_rebuild()
except NameError:
    pass
